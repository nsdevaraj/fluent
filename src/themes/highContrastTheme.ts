import { teamsHighContrastTheme } from "@fluentui/react-components";

/**
 * High-contrast theme built on Fluent UI v9 `teamsHighContrastTheme`.
 * Pass to `<FluentProvider theme={highContrastTheme}>` to enable high-contrast mode.
 */
export const highContrastTheme = teamsHighContrastTheme;
